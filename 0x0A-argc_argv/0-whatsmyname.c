#include <stdio.h>

/**
 * main - Entry point, prints program's name, followed by a new line
 * @argc: Number of arguments
 * @argv: array of argument strings
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
